# Component Patterns

## Composition Over Inheritance

Prefer composition patterns (render props, compound components) over deep inheritance hierarchies.

## Container/Presenter Split

Separate data-fetching logic (containers) from rendering logic (presenters) to improve testability and reuse.

## Error Boundaries

Wrap major UI sections in error boundaries to prevent cascading failures.
